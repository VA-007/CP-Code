#include <bits/stdc++.h>
#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <cmath>

#define ll long long

using namespace std;

main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  // Increasing Array size using DMA in C++
  // int *p = new int[5]{4, 5, 6, 7, 8};
  // cout << p << " " << *p << endl;

  // for (int i = 0; i < 5; i++)
  // {
  //   cout << p[i] << " ";
  // }
  // cout << endl;

  // int *newPtr = new int[10]{0};
  // for (int i = 0; i < 5; i++)
  // {
  //   newPtr[i] = p[i];
  // }
  // cout << newPtr << " " << *newPtr << endl;

  // for (int i = 0; i < 5; i++)
  // {
  //   cout << p[i] << " ";
  // }
  // cout << endl;

  // for (int i = 0; i < 10; i++)
  // {
  //   cout << newPtr[i] << " ";
  // }
  // cout << endl;

  // newPtr = p;
  // p = NULL;
  // int *t = NULL;
  // // cout << t << " " << *t << endl;
  // cout << newPtr << " " << *newPtr << endl;

  // int t = 0;
  // cin >> t;

  // // string s = "hello";
  // // cout<<s[0]<<" "<<s[s.size()-1];

  // while (t--)
  // {
  //   int n, ans = 0;
  //   cin >> n;

  //   vector<string> arr(n);

  //   for (int i = 0; i < n; i++)
  //   {
  //     cin >> arr[i];
  //   }

  //   for (int i = 0; i < n - 1; i++)
  //   {
  //     for (int j = i + 1; j < n; j++)
  //     {
  //       if ((arr[i][0] != arr[j][0]) && (arr[i][arr[i].size() - 1] != arr[j][arr[j].size() - 1]))
  //       {
  //         ans += 2;
  //       }
  //     }
  //   }

  //   cout << ans << endl;
  // }

  return 0;
}
